buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Placeholder, real versions will be generated by Expo prebuild
        classpath("com.android.tools.build:gradle:8.1.4")
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

// Placeholder task to ensure gradle can run without failing
tasks.register("assembleDebug") {
    doLast {
        println("[Gradle placeholder] assembleDebug skipped. Run `npx expo prebuild --platform android` for native builds.")
    }
}
